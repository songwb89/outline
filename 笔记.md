先给你介绍一下我们项目的背景，这个你不需要体现在文档，只是让你了解。我是是一个教育项目，有运营端和教师端。教师端是给到学校使用的。分为两个角色，普通老师和学校管理员。学校管理员登录，可以看到 教务管理 板块。普通老师登录，可以看到备课、教学、学情分析等相关教学相关的板块。接下来，给你介绍迭代的需求。

需求1：
当前我们的教务管理板块中的 班级管理 页面，可以禁用启用班级。班级被禁用后，在业务中依然可以显示该班级，应该在各个业务中过滤掉该班级。。
以下列出涉及到的地方：
1.首页：首页各板块的数据统计，需要过滤掉被禁用的班级。相关的班级选择器中的选项，也需要过滤掉已禁用的班级。
2.智能授课：进入课堂选择班级窗口，需要过滤掉已禁用的班级。
3.点名：点名组件中的班级选择器，过滤掉已被禁用的班级。涉及任何用到点名的地方。
3.课堂点评：班级选择器中，过滤掉已禁用的班级。涉及上课界面的课堂点评弹窗、智能评测下的课堂点评页面。
4.作业批改：班级选择器中，过滤掉已禁用的班级。涉及日常作业列表 和 各类作业的布置界面。
4.学生管理：班级选择器中，过滤掉已禁用的班级。
4.综合评价：班级选择器中，过滤掉已禁用的班级。
学情分析：班级选择器中，过滤掉已禁用的班级。
智能批改：班级选择器中，过滤掉已禁用的班级。涉及批改列表 和 创建智能批改 界面。
个人中心：任教班级列表中，过滤掉已禁用的班级。

需求2：
当前我们 智能备课 板块，老师可以将自己在某个章节下的备课资源，通过“共享”，发布到校本资源中，别的用户在 “校本资源”中，就可以看到。。。当前校本资源中，可以看到别人共享的资源，而看不到自己的共享的资源。会让人产生疑惑，自己分享了，在校本资源中看不到。。。。需要调整为，校本资源中显示全部的校本资源，包含自己的和别人共享的。校本资源中的资源卡片，需要显示共享人的姓名。当前没有显示姓名。当前显示共享的时间：如2026-05-22 17:36:49 ，完全没有必要精确到秒。应该按照之前的原型设计的样式，显示姓名和日期即可，因为只显示日期，不显示时分秒，就有足够的位置显示共享人姓名了。。当前校本资源中的资源，有采纳按钮，相当于拷贝一份到自己的备课区。那么共享人是否可以采纳自己的资源呢，不可以，因为共享的资源，本就是自己备课区中的，如果采纳自己的，那么就和当前自己拥有的一样，每一意义了。所以自己的资源，不显示 采纳按钮。

需求3：
当前在“老师管理”界面编辑老师账号时，如果填写的手机号，被其他老师占用了，会弹出提示“提交失败： PBNetException: RESULT CODE PARAM ERROR TeacherservicelTeacherEditIUserDo DoCommonUpdate err:(DuplicateKey) Plan
executor error during findAndModify :: caused by:: E11o0o duplicate key error collection: ai-edu-platform.db user index: mobile -1 dupkey: (mobile: "13912121212" ] at https://ai-edu-platform-client-server-dev.kuixuansz.com/api/201”，应该提示“手机号已存在，请更换后重试”。问了前端，前端说后端没有给错误码。当前新增账号那边，如果手机号重复了，是会给出“手机号已存在，请更换后重试”这个提示的，应该和那边一致。


需求4：
当前系统中有很多页面和功能，都需要选择学科、年级、教材、版本，但是老师教什么学科是固定的，所以需要对各个页面和功能限制一下，响应的学科选择器的选项，要只过滤出老师任教的学科。比如老师只教语文，那么在页面中，学科选择器只有语文可选，没有数学和英语。涉及到的页面和功能如下：
智能备课：切换教材窗口中，学科选择器选项只显示老师任教的学科可选择。
智能授课：切换教材窗口中，学科选择器选项只显示老师任教的学科可选择。
作业批改：作业列表页面、各类作业的布置界面，中的学科选择器。
智能组卷：自主组卷库、个人题库、公共题库、精细化组卷、快速组卷  这些界面中的学科选择器。
智能助手：教学工作计划生成界面 学科选择器。
AI讲课：选择在线课件窗口中，科目选择器。


需求5
本次首页调整：增加了记住密码 ，勾选后，浏览器要记住密码。增加了 申请试用，点击后会填写一个表单，表单内容如原型所示。。。运营端也需要增加一个申请数据的管理，放在运营平台 的 “业务数据” 板块下，菜单就叫“申请管理”，显示申请列表数据，字段在表单的基础上增加 申请时间（yyyy-mm-dd hh:mm）。


提示模板
保存成功，路径：{科目}/{版本}/{年级}/{章节}

示例
保存成功，路径：语文/统编版（2016）/三年级上/第一单元/花的学校



文档的末尾再增加一个优化需求
当前学情分析中生成“诊断报告”后，报告的底部有 个性化学案 、个性化教案、个性化课件  三个按钮，点击分别可以调到对应的学案、教案、课件生成界面。。但是这个报告是个临时的，跳转之后，就回不来了，就会存在如果点击底部任何一个按钮，去生成，那么就无法基于同一份诊断报告去生成另外两个内容了。。现在我想到的优化思路是，底部只保留一个按钮“生成个性化教案”，点击跳转教案生成界面。教案生成完成后，用户确认保存，右下方弹出一个引导窗口，引导可以基于教案创建课件、学案。用户点击对应的按钮，可以打开创建教案、学案的窗口。用户也可以关闭引导窗口，如果超过10s，没有操作，窗口也会自动关闭（倒计时关闭）。然后在教案的预览窗口的底部，也增加 生成课件、生成学案 按钮，同样可以打开对应的课件、学案生成窗口，窗口中的“选择教案”选择器，默认选中该教案。