## 首页
- 标题
- 作者

## 目录页
- 目录 （卡片，4个）

## 章节首页
- 序号
- 主标题
- 副标题

## 章节内容页
- 标题
- 内容
- 配图




当前第二行备课相关的图表有两个，现在需要改造，可以去掉这两个，新增6个，分成两行，每行三个。、
第一行从资源新增的维度，展示如下图表：
1. 新增资源统计（柱状图），显示全部类型的资源的新增数量，每个柱子表示一种类型的资源。
2. 新增资源趋势（折线图），包含一个资源类型的选择器，默认全部，可以切换，折线图显示的就是在不同时间的所选类型资源的新增数量趋势。
3. 教师备课活跃度分布（柱状图），x轴表示创建资源色数量区间，Y轴表示老师数量。

第二行从资源使用的维度，展示如下图表：
1. 资源使用统计(柱状图)，显示全部类型的资源被使用的次数，每个柱子表示一种类型的资源。
2. 资源使用趋势(折线图)，包含一个资源类型的选择器，默认全部，可以切换，折线图显示的就是在不同时间的所选类型资源的使用次数趋势。
3. 教师资源使用活跃度分布（柱状图），x轴表示资源被使用的次数区间，Y轴表示老师数量。













