现在我需要你写一个“个性化备课及辅导.md”的需求文档。文档中要将需求说清楚，能用表格梳理的可以用表格，能用流程图的就用plantuml，一些异常情况的提示什么的都要包含，但语言和结构要简洁，避免重复啰嗦。以下由我先给你描述需求，然后你基于我的描述分析并书写文档。你有不清楚的，需要确认的地方，也可以向我提问了解。

这次需求调整
1. 去掉了AI专区 中的 “AI讲课”和“AI讲题”功能入口。原本这两个功能是独立的。现在从AI专区板块下去掉了，然后将其融入现有的备课授课体系中。具体体现如下：
1.1 在 智能备课 界面，备课工具增加“智能辅导”下拉菜单，点击显示 AI讲解、AI讲课 两个菜单。点击 AI讲解 按钮，弹出窗口 让用户输入需要讲解的问题或知识点，也可以上传图片，点击 生成讲解 ，弹出生成窗口，显示生成过程。这个功能和原有的AI专区 中的 AI讲题 流程上完全一样，只是之前的问题输入、讲解过程、讲解预览，都是页面，在这边改成了弹窗，前端要将之前的功能改造封装成弹窗的形式。AI讲解生成完成后，会自动切到预览，这个还是同之前一样，但是初始状态为草稿，预览界面的右上方需要显示“确认保存”按钮，用户点击确认后，状态才变为正常，右上角显示“分享”按钮，不再有收藏按钮。若点击 AI讲课 则需要判断当前章节下，是否有课件，如果没有，则弹出toast提示，告知缺少课件，因为AI讲课是基于课件生成讲解的，这个toast提示语，你给一个简单明了的。如果有课件，则弹出选择需要讲解的课件窗口，用户可以选择课件。如果存在很多课件，页面可以滚动展示。选择课件后，就会弹出课件生成窗口，这个窗口的内容，还是和AI专区中的 AI讲课 的界面内容一样，只是那边是页面，这边是弹窗，这里前端需要将那边的页面封装成弹窗。之前上传课件或选择本地课件后，只是自动生成课件讲解，思维导图、视频概率，都是需要用户手动去创建的，在这边不需要手动去点，用户选择课件后，课件讲解、思维导图、视频概览 三项都会自动生成，用户关闭窗口不影响生成，因为还是和之前一样，是异步任务。当三项全部生成完成，则讲课的状态变为“草稿”，右上角显示一个“确认保存”按钮，用户点击后，状态切换为正常。当三项中存在生成中状态和已完成状态，整个讲个状态就是 生成中，如果三项中存在一项失败，则整个讲课状态为 生成失败，如果三项都生成完成，但没有确认，则为草稿，确认后为正常。。
1.2 在 智能备课 界面 的资源列表卡片中，也需要增加 AI讲课 和AI讲解  两类卡片，操作菜单项有 重命名、共享（状态为正常才有）、删除。点击卡片，会打开对应的预览弹窗。
1.3 校本资源 弹窗中，也需要增加AI讲课 和 AI讲解 这两种资源，上方的筛选标签也增加这两种。。
1.4 一键生成内容 窗口 需要增加 AI讲解 选项。可以一键生成内容。这块需要通过一键备课的工作流实现，会基于教材节点，生成适合的问题，再基于对应的问题生成讲解。

2.学情诊断报告增加 “个性化备课”功能按钮。点击 该按钮，弹出“个性化备课”窗口，窗口内需要选择备课的 年级、教材版本、章节，还需要选择备课内容（默认选中 教案、学案、课件、随堂测，用户可以调整选中），这个界面中，会显示每个内容需要点点数，以及预计消耗的总点数，然后点击开始生成，就会弹内容正在生成 的弹窗提示窗口。这里的逻辑和，智能备课那边的一键生成内容是一样的，唯一的区别是，这里需要手动选择年级、教材版本、章节，还有通过这里进行备课内容的生成，会将 薄弱知识点 作为参数传到一键备课工作流。

3.学情诊断报告增加“个性化辅导”，如果是单个学生的学情诊断报告，点击 个性化辅导 按钮，则直接触发 生成AI讲解的异步任务，并提示内容正在生成，稍后请前往智能评测下的个性化辅导中查看，这里注意点是，采用异步生成，和智能备课界面由前端调用NPC进行生成不同，这里的逻辑是将报告中的薄弱知识点 传给一个模型任务，去提炼问题，然后将问题作为参数生成AI讲解，因为是针对的单个学生生成讲解，且不是针对章节，是针对知识点，所以生成结果直接存到 智能评测 模块下的 个性辅导 中。如果是班级的学情诊断报告，点击个性化辅导，则会弹出个性化辅导的弹窗，这个弹窗需要用户选择 辅导场景，场景有 课中演示、课后辅导，默认选中课中演示，，如果选中课中演示，则下方需要选择对应的章节（年级、教材、章节），因为生成的内容需要存到对应章节下，如果选择课后辅导，则下方隐藏 章节选择，生成的内容会保存到 智能评测 下的 个性化辅导 中。。。

4. 智能评测板块下需要增加一个 个性辅导 菜单页面，显示通过学情报告生成的 非课中演示 的AI讲解内容。卡片内容展示 标题、日期、适用范围（如果是通过班级）。状态 生成中，显示生成中，失败显示生成失败，生成完成后，初始状态是草稿，用户确认后，状态变为正常。只有状态正常时，预览界面的 分享 按钮才显示。收藏按钮去掉。






会议主题：AI讲课/AI讲题移至智能备课 & 基于学情报告进行个性化备课和辅导
会议时间：2026.6.16 10:00~11:30
参会人员：@Suki Zhong(Suki Zhong)@Tavin Xiao(Tavin Xiao)@Kobe Li(kobe)@Wes Wu(Wes)@Vincent Chen(vincent)@Charls Chen(Charls)@Owen Wang(Owen Wang)@Nicole Li(Nicole)@Kimi Hu(Kimi hu)@Ian Liu(Ian)
会议地点：D会议室
原型链接：http://10.16.0.86/8.0/