# 课堂分析工具 - 产品需求文档

> 本文档记录产品实现步骤，用于与开发团队沟通。

> 更新时间：2026-05-29

> 当前进度：已完成步骤1、2、3、4、5、6、7

---

## 功能概述

将课堂录音/视频自动转成带角色标注的文字稿，方便后续分析学生学习情况。

---

## 实现步骤

### 步骤1：音频识别（语音转文字）

**做什么**：把用户上传的音视频文件，转换成文字内容。

**用户操作**：

1. 用户上传课堂录音（mp3/wav/m4a）或课堂视频（mp4/avi）
2. 系统自动处理，生成文字稿

**处理流程**：

```
用户上传文件
    ↓
如果是视频 → 用工具提取音频
    ↓
调用火山引擎ASR服务（豆包大模型版）
    ↓
生成原始识别结果（JSON格式）
```

**输出**：原始识别JSON文件，包含所有识别出的文字和时间戳

**技术说明**（供开发参考）：

- 视频格式使用 FFmpeg 提取音频
- 使用火山引擎 `volc.bigasr.auc` 服务
- 模型名称：`bigmodel`（大模型版）
- 配置参数：开启标点预测、逆文本归一化、说话人分离

---

### 步骤2：数据预处理与角色识别

**做什么**：把原始识别结果整理干净，识别角色，保留逐字数据用于后续分析。

**处理流程**：

```
原始识别JSON
    ↓
清理数据（去除additions嵌套）
    ↓
提取包含多个说话人的对话片段
    ↓
调用AI大模型（阿里千问）判断哪个是老师
    ↓
保留逐字数据（words数组）
    ↓
生成带角色标注的完整数据
```

**输出**：处理后JSON文件

**数据结构示例**：

```json
{
  "utterances": [
    {
      "start_ms": 20890,
      "end_ms": 24930,
      "text": "已经在的同学打开书，背一下春江花月夜啊，自己。",
      "role": "teacher",
      "words": [
        {"word": "已", "start_ms": 20890, "end_ms": 21130},
        {"word": "经", "start_ms": 21130, "end_ms": 21250}
      ]
    }
  ],
  "metadata": {
    "total_duration_ms": 2634070,
    "total_utterances": 556,
    "total_words": 7534
  }
}
```

**字段说明**：


| 字段       | 类型     | 说明                              |
| -------- | ------ | ------------------------------- |
| start_ms | int    | 开始时间（毫秒）                        |
| end_ms   | int    | 结束时间（毫秒），时长 = end_ms - start_ms |
| text     | string | 完整句子文本                          |
| role     | string | teacher / student               |
| words    | array  | 逐字数据（用于语速分析）                    |


---

### 步骤3：教学环节分析

#### 3.1 功能描述

**做什么**：基于课堂文字稿，自动识别并划分教学环节结构，生成 AI 分析评价。

**数据来源**：步骤2输出的 `utterances` 数组（需先提取不含 words 的简化版本）

**处理流程**：

```
utterances数组
    ↓
提取简化版本（去除words，只保留role/text/start_ms/end_ms）
    ↓
调用AI大模型（阿里千问）
    ↓
识别教学环节结构
    ↓
生成带时间戳的环节分析
```

**数据提取示例**（给模型用）：

```javascript
// 从完整数据中提取简化版本
const simplified = utterances.map(u => ({
  role: u.role,
  text: u.text,
  start: u.start_ms,
  end: u.end_ms
}));
```

---

#### 3.2 AI 提示词

##### 系统提示词

```
你是一个专业的教学分析专家。

【核心任务】
分析课堂对话数据，识别教学环节结构。

【数据说明】
输入是课堂对话JSON，每条记录包含：
- role: "teacher" 或 "student"
- text: 说话内容
- start: 开始时间（毫秒）
- end: 结束时间（毫秒）

【输出格式】（严格遵守）
{
  "teaching_phases": [
    {
      "name": "主环节名称",
      "details": [
        {
          "name": "子环节名称",
          "description": "子环节描述",
          "start_ms": 开始时间,
          "end_ms": 结束时间
        }
      ]
    }
  ],
  "ai_analysis": {
    "comment": "AI点评",
    "suggestion": "改进建议，可为null"
  }
}

【关键规则】
1. 子环节必须连续无间隙，details[i].end_ms = details[i+1].start_ms
2. 所有子环节时长之和 = 主环节时长
3. 所有主环节的最后一个子环节的结束时间 = 整个课堂结束时间
4. 环节划分要体现教学逻辑
5. comment必须有，suggestion可选（无建议时填null）

【长度限制】
- 主环节名称：4-15字
- 子环节名称：4-15字
- 子环节描述：20-200字
- AI点评：30-150字
- 改进建议：30-100字（无建议时填null）
```

##### 用户提示词

```
请分析以下课堂对话数据，识别教学环节。

【课堂对话数据】
{{transcript}}

【学科】（可选）: {{subject}}
【年级】（可选）: {{grade}}

请严格按系统提示词中的格式输出JSON，不要包含任何其他内容。
```

---

#### 3.3 输出数据结构（AI生成）

**JSON 结构示例**：

```json
{
  "teaching_phases": [
    {
      "name": "课堂导入与诵读",
      "details": [
        {
          "name": "课前准备与礼仪",
          "description": "教师组织学生课前背诵《春江花月夜》，随后进行上课起立问好等常规课堂礼仪。",
          "start_ms": 20890,
          "end_ms": 214770
        },
        {
          "name": "复习旧知与朗诵",
          "description": "教师引导学生回顾上节课学习内容，组织全班有感情地朗诵诗歌前半部分。",
          "start_ms": 214770,
          "end_ms": 308120
        }
      ]
    },
    {
      "name": "月下之情意象探究",
      "details": [...]
    }
  ],
  "ai_analysis": {
    "comment": "本课教学逻辑严密，从诵读导入到意象深挖，层层递进...",
    "suggestion": "小组讨论环节时间较短，建议增加讨论时长..."
  }
}
```

---

#### 3.4 前端渲染需求

##### 页面展示效果

```
┌─────────────────────────────────────────────────────────────────┐
│ 教学环节                                           [AI分析]     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 📋 课堂导入与诵读                              4'45'' 10.9%│ │
│ │   0:20~3:34  课前准备与礼仪                           │ │
│ │           教师组织学生背诵诗歌，进行课堂礼仪             │ │
│ │   3:34~5:08  复习旧知与朗诵                           │ │
│ │           回顾上节课内容，全班朗诵诗歌前半部分           │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 📋 月下之情意象探究                            14'47'' 35.2%│ │
│ │   ...                                                      │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

##### 计算规则


| 内容    | 计算方式                                                          |
| ----- | ------------------------------------------------------------- |
| 主环节时长 | 该主环节下所有子环节时长之和：`sum(details[i].end_ms - details[i].start_ms)` |
| 时长占比  | 主环节时长 / 课堂总时长 × 100%，保留1位小数                                   |
| 时长显示  | 毫秒转换为 `M'SS''` 格式，如 `4'45''`                                  |
| 时间显示  | 毫秒转换为 `M:SS` 格式，如 `0:20`                                      |
| 总时长   | 最后一个子环节的 `end_ms`                                             |


##### 时间格式转换

```javascript
// 毫秒 → 时长显示（4'45''）
function formatDuration(ms) {
  const totalSeconds = Math.floor(ms / 1000);
  const minutes = Math.floor(totalSeconds / 60);
  const seconds = totalSeconds % 60;
  return `${minutes}'${seconds.toString().padStart(2, '0')}''`;
}

// 毫秒 → 时间显示（0:20）
function formatTime(ms) {
  const totalSeconds = Math.floor(ms / 1000);
  const minutes = Math.floor(totalSeconds / 60);
  const seconds = totalSeconds % 60;
  return `${minutes}:${seconds.toString().padStart(2, '0')}`;
}
```

##### 交互要求


| 交互           | 行为                              |
| ------------ | ------------------------------- |
| 点击主环节展开/折叠图标 | 展开/折叠子环节列表                      |
| 点击主环节标题/播放图标 | 打开音频播放器，跳转至该主环节起始时间播放           |
| AI分析区        | 显示 comment 和 suggestion，支持折叠/展开 |


##### 字段长度校验（开发可不做，但提示词已约束）


| 字段    | 要求长度         |
| ----- | ------------ |
| 主环节名称 | 4-15字        |
| 子环节名称 | 4-15字        |
| 子环节描述 | 20-200字      |
| AI点评  | 50-150字      |
| 改进建议  | 50-150字或null |


---

### 步骤4：课堂结构分析（S-T / Rt-Ch）

#### 4.1 功能描述

**做什么**：基于预处理后的 utterances 数据，计算 S-T 和 Rt-Ch 指标，绘制行为分析图表，并判断课堂类型。

**数据来源**：步骤2输出的 `utterances` 数组

**处理流程**：

```
utterances数组
    ↓
S-T分析（每分钟采样）
    ↓
Rt-Ch分析（每10秒采样）
    ↓
计算指标并判断课堂类型
    ↓
绘制图表 + 生成AI分析
```

---

#### 4.2 S-T 行为分析图

##### 采样规则


| 参数   | 值        | 说明        |
| ---- | -------- | --------- |
| 采样间隔 | 60秒（1分钟） | 每隔1分钟采样一次 |
| 累计计算 | 从0开始     | 逐步累加师生时长  |


##### 计算公式

```
采样点 i (i = 0, 1, 2, ...):
  T累计(i) = sum(教师发言时长)，从0到采样点i
  S累计(i) = sum(学生发言时长)，从0到采样点i

图表坐标点:
  x = T累计(i) / 60000  // 转分钟
  y = S累计(i) / 60000  // 转分钟
```

##### 图表样式

```
Y（学生累计时长，分钟）
  │
20 ┤              ●━━●
  │            /
10 ┤          /
  │        /
 0 ┼──────────────────→ X（教师累计时长，分钟）
 0    5    10   15   20
```

---

#### 4.3 Rt-Ch 行为分析图

##### 采样规则


| 参数   | 值          | 说明         |
| ---- | ---------- | ---------- |
| 采样间隔 | 30秒        | 每隔30秒采样一次  |
| 采样方式 | 判断该时间点谁在说话 | 取说话时间最长的角色 |


##### 计算公式

```
采样得到序列：[T, T, S, S, S, T, ...]（T=教师，S=学生）

N = 采样总数（发言段数量）
NT = 教师发言段数
Rt = NT / N  // 教师行为占有率

g = 连续行为组数量（如：TT | S | T | SS = 4组）
Ch = (g - 1) / N  // 师生转化率
```

##### 图表样式

```
         Ch (转化率)
            │
         1.0 ┤      ╱对话型
            │     ╱ ╲
         0.6 ┤   ●───●
            │   ╱│   │╲
            │  ╱ │混合│ ╲
         0.4 ┤ ╱  │型  │ ╲
            │╱   │   │   ╲
            └──────────────────→ Rt (占有率)
              0   0.3  0.5  0.7  1.0
              └──┬──┘   └───┬──┘
              练习型       讲授型
```

##### 四种课堂类型


| 课堂类型 | 划分标准 (Rt 与 Ch 的数值范围)      | 课堂特征                            |
| ---- | ------------------------- | ------------------------------- |
| 讲授型  | Rt ≥ 0.7                  | 教师行为占比极高，课堂以教师单向讲授为主，师生互动较少。    |
| 练习型  | Rt ≤ 0.3                  | 教师行为占比很低，课堂时间主要留给学生进行自主练习或活动。   |
| 对话型  | Ch ≥ 0.4                  | 师生行为转换非常频繁，课堂上师生之间有充分的问答与交流互动。  |
| 混合型  | 0.3 < Rt < 0.7 且 Ch < 0.4 | 教师讲授与学生参与的时间分配较为均衡，既有讲解也有适度的互动。 |


##### 图表区域坐标（前端渲染用）


| 类型  | 形状  | 坐标点                                                     | 背景色 |
| --- | --- | ------------------------------------------------------- | --- |
| 练习型 | 三角形 | (0,0) → (0.3,0) → (0.3,0.6)                             | 浅绿色 |
| 混合型 | 正方形 | (0.3,0) → (0.7,0) → (0.7,0.4) → (0.3,0.4)               | 浅灰色 |
| 讲授型 | 三角形 | (0.7,0) → (1,0) → (0.7,0.6)                             | 浅蓝色 |
| 对话型 | 五边形 | (0.3,0.4) → (0.7,0.4) → (0.7,0.6) → (0.5,1) → (0.3,0.6) | 浅橙色 |


##### 判断逻辑

```javascript
function getClassType(Rt, Ch) {
  if (Rt >= 0.7) return '讲授型';
  if (Rt <= 0.3) return '练习型';
  if (Ch >= 0.4) return '对话型';
  return '混合型';
}
```

##### 类型展示

在 Rt-Ch 图上显示数据点和课堂类型标签：

- **数据点**：在坐标 (Rt, Ch) 处显示一个散点
- **类型标签**：在图表下方显示 `课堂类型：{类型名}`

```
┌─────────────────────────────────────┐
│  图表区域（显示数据点 ●）            │
│                                      │
│                              ● (Rt=0.75, Ch=0.12)
│                                      │
└─────────────────────────────────────┘
课堂类型：讲授型
```

---

#### 4.5 AI 分析模板

##### 分析内容

根据师生占比关系，选用对应模板拼接：

**教师主导（教师占比 > 学生占比 + 5%）**

```
本节课教师行为时长（占比{教师占比}%）大于学生行为时长（占比{学生占比}%），表明本节课是以教师主导的课堂，本节课Rt={Rt值}，Ch={Ch值}，整体表现为{课堂类型}。
```

**学生主导（教师占比 < 学生占比 - 5%）**

```
本节课教师行为时长（占比{教师占比}%）小于学生行为时长（占比{学生占比}%），表明本节课是以学生为中心的课堂，本节课Rt={Rt值}，Ch={Ch值}，整体表现为{课堂类型}。
```

**师生均衡（|教师占比 - 学生占比| ≤ 5%）**

```
本节课教师行为时长（占比{教师占比}%）接近学生行为时长（占比{学生占比}%），表明本节课是师生互动均衡的课堂，本节课Rt={Rt值}，Ch={Ch值}，整体表现为{课堂类型}。
```

##### 变量说明


| 变量   | 计算方式                | 示例   |
| ---- | ------------------- | ---- |
| 教师占比 | 教师累计时长 / 总时长 × 100% | 75%  |
| 学生占比 | 学生累计时长 / 总时长 × 100% | 25%  |
| Rt值  | NT / N，保留2位小数       | 0.75 |
| Ch值  | (g-1) / N，保留2位小数    | 0.12 |
| 课堂类型 | 根据判断规则判断            | 讲授型  |


---

### 步骤5：知识点结构与分析

#### 5.1 功能描述

**做什么**：基于课堂对话记录，结合学科知识树，分析本节课涉及的知识点，生成知识点树结构及每个知识点的详细描述。

**数据来源**：

1. 步骤2输出的 `utterances` 数组（简化版，去掉 words）
2. 用户上传音视频时选择的年级和学科对应的知识树 JSON

**处理流程**：

```
步骤2输出 utterances 数组
    ↓
提取简化版本（去除words，只保留role/text/start_ms/end_ms）
    ↓
根据用户选择的学科和年级，获取对应的知识树
    ↓
调用AI大模型（阿里千问）
    ↓
识别知识点结构 + 每个知识点的描述和时间段
```

---

#### 5.2 AI 提示词

##### 系统提示词

```
你是一个专业的教学分析专家。

【核心任务】
分析课堂对话数据，从知识树中选择本节课涉及的知识点，构建知识点树结构，并记录每个知识点的讲解时间段。

【数据说明】
1. 课堂对话JSON（简化版），每条记录包含：
   - role: "teacher" 或 "student"
   - text: 说话内容
   - start: 开始时间（毫秒）
   - end: 结束时间（毫秒）

2. 学科知识树JSON，树状结构，层级为：学科 → 一级知识域 → 二级知识模块 → 三级知识专题 → 四级知识点。

【核心要求】
1. 知识点定位精准：找到与课堂内容最直接相关的知识树节点作为顶级
2. 子集丰富：挖掘所有与课堂内容相关的知识点，不仅限于直接提到的
3. 包含所有层级：knowledgeTree 中的每个节点都需要在 knowledgePoints 中有对应条目
4. 记录时间段：每个知识点记录课堂中讲解该知识点的起始和结束时间（毫秒）
5. 时间段可重合：不同知识点的时间段没有互斥限制，可能在同一时段讲解多个知识点

【时间戳记录规则】
1. start_ms：该知识点在课堂中首次讲解的起始时间（毫秒）
2. end_ms：该知识点在课堂中讲解的结束时间（毫秒）
3. 各节点时间独立：父子节点的时间段各自独立记录，不做强制关联

【输出格式】（严格遵守）
{
  "knowledgeTree": {
    "label": "知识点名称",
    "children": [
      { "label": "子知识点", "children": [...] }
    ]
  },
  "knowledgePoints": {
    "知识点名称": {
      "start_ms": 12345,
      "end_ms": 234567,
      "description": "知识点描述，10-60字",
      "teaching_activities": "教学活动，5-60字",
      "teaching_content": "教学内容，5-60字",
      "teaching_methods": "教学方法，5-60字",
      "common_mistakes": "易错点，必须以'学生可能'开头，5-60字"
    }
  }
}

【知识点分析思路】
1. 找出课堂中明确讲解的概念和题目类型
2. 从知识树中找到对应的知识点节点
3. 向上追溯找到最精准的顶级节点（不是学科名，而是本节课主题）
4. 向下扩展，挖掘该知识点下所有相关的子知识点
5. 根据对话内容，判断每个知识点的时间段

【关键规则】
1. 知识点定位精准：找到与课堂内容最直接相关的知识树节点作为顶级
2. 子集丰富：挖掘所有与课堂内容相关的知识点，不仅限于直接提到的
3. 包含所有层级：knowledgeTree 中的每个节点都需要在 knowledgePoints 中有对应条目
4. 时间戳独立：每个知识点记录讲解的起始和结束时间（毫秒），时间段允许重合
5. knowledgeTree 必须从输入的知识树中选择节点，保持父子关系不变
6. 只选择本节课实际涉及的知识点，不要虚构
7. common_mistakes 必须以"学生可能"开头，如："学生可能在去分母时忘记同时乘以每一项"
```

##### 用户提示词

```
请分析以下课堂对话数据，从知识树中识别本节课涉及的知识点。

【课堂对话数据】
{{transcript}}

【知识树】
{{knowledgeTree}}

请严格按系统提示词中的格式输出JSON，不要包含任何其他内容。
```

---

#### 5.3 输出数据结构（AI生成）

**JSON 结构示例**：

```json
{
  "knowledgeTree": {
    "label": "一元二次方程",
    "children": [
      {
        "label": "一元二次方程的相关概念",
        "children": [
          { "label": "一元二次方程的定义" },
          { "label": "一元二次方程的一般形式" }
        ]
      },
      {
        "label": "实际问题与一元二次方程",
        "children": [
          { "label": "几何问题" },
          { "label": "增长率问题" }
        ]
      }
    ]
  },
  "knowledgePoints": {
    "一元二次方程": {
      "start_ms": 5000,
      "end_ms": 2400000,
      "description": "本节课主要学习一元二次方程的概念、解法及其实际应用...",
      "teaching_activities": "通过实际问题引入，引导学生发现方程模型...",
      "teaching_content": "一元二次方程的定义、一般形式、解的概念...",
      "teaching_methods": "采用问题驱动教学法，从具体问题抽象出数学模型...",
      "common_mistakes": "学生可能忽略二次项系数不能为零的条件..."
    },
    "一元二次方程的相关概念": {
      "start_ms": 8000,
      "end_ms": 600000,
      "description": "系统讲解一元二次方程的基本概念和定义...",
      "teaching_activities": "通过例题辨析，加深学生对概念的理解...",
      "teaching_content": "定义、一般形式ax²+bx+c=0(a≠0)、解的概念...",
      "teaching_methods": "讲授法为主，配合多媒体演示...",
      "common_mistakes": "学生常忘记二次项系数不能为零的条件..."
    },
    "一元二次方程的定义": {
      "start_ms": 8000,
      "end_ms": 180000,
      "description": "通过实例引入一元二次方程的定义...",
      "teaching_activities": "学生观察方程特点，总结规律...",
      "teaching_content": "含有一个未知数，且未知数的最高次数为2的整式方程...",
      "teaching_methods": "观察法、归纳法...",
      "common_mistakes": "学生容易将二次项系数写成0..."
    }
  }
}
```

**字段说明**：


| 字段                  | 类型     | 说明                    |
| ------------------- | ------ | --------------------- |
| knowledgeTree       | object | 知识点树状结构               |
| knowledgePoints     | object | 每个知识点的详细信息            |
| start_ms            | int    | 该知识点首次讲解的起始时间（毫秒）     |
| end_ms              | int    | 该知识点讲解的结束时间（毫秒）       |
| description         | string | 知识点描述，10-60字          |
| teaching_activities | string | 教学活动，5-60字            |
| teaching_content    | string | 教学内容，5-60字            |
| teaching_methods    | string | 教学方法，5-60字            |
| common_mistakes     | string | 易错点，必须以"学生可能"开头，5-60字 |


---

### 步骤6：教师语速分析

#### 6.1 功能描述

**做什么**：基于预处理后的 utterances 数据，计算教师的总字数、授课时长、平均语速，并绘制每分钟的语速折线图。

**数据来源**：步骤2输出的 `utterances` 数组

**处理流程**：

```
utterances数组
    ↓
过滤出所有 role="teacher" 的记录
    ↓
计算总字数、授课时长、平均语速
    ↓
每分钟采样，计算每分钟语速
    ↓
绘制语速折线图 + 生成AI分析
```

---

#### 6.2 计算规则

##### 数据计算


| 指标   | 计算方式                                                              |
| ---- | ----------------------------------------------------------------- |
| 总字数  | 所有 teacher 发言的 text 字符数之和                                         |
| 讲授时长 | 所有 teacher 发言段的时长之和 = sum(teacher.end_ms - teacher.start_ms)，转为分钟 |
| 平均语速 | 总字数 / 讲授时长（字/分钟），保留整数                                             |


##### 语速折线图采样


| 参数   | 值        | 说明                  |
| ---- | -------- | ------------------- |
| 采样间隔 | 1分钟      | 每隔1分钟采样一次           |
| X轴   | 时间（分钟）   | 0', 1', 2', ..., N' |
| Y轴   | 语速（字/分钟） | 该分钟内的教师说话字数         |


##### 采样计算方式

```
第 n 分钟的语速：
  - 时间范围：(n-1)*60000 毫秒 ~ n*60000 毫秒
  - 该范围内所有 teacher 发言的字数之和
  - 如果该分钟没有老师发言，语速为0
```

---

#### 6.3 数据结构（后端计算）

**JSON 结构示例**：

```json
{
  "speakingSpeed": {
    "totalWords": 7028,
    "totalDuration": 31.8,
    "avgSpeed": 220,
    "chartData": [
      { "time": 0, "speed": 180 },
      { "time": 1, "speed": 182 },
      { "time": 2, "speed": 185 }
    ]
  }
}
```

**字段说明**：


| 字段                | 类型    | 说明              |
| ----------------- | ----- | --------------- |
| totalWords        | int   | 教师授课总字数         |
| totalDuration     | float | 讲授时长（分钟，保留1位小数） |
| avgSpeed          | int   | 平均语速（字/分钟）      |
| chartData         | array | 每分钟的语速数据        |
| chartData[].time  | int   | 时间点（分钟）         |
| chartData[].speed | int   | 该分钟的语速（字/分钟）    |


---

#### 6.4 前端渲染模板

##### 分析文本

前端使用以下模板展示分析文本：

```
本节课教师在授课过程中一共说了{{totalWords}}个字，共耗时{{totalDuration}}分钟，平均语速为{{avgSpeed}}字/分钟。课堂语速的范围推荐保持在150字/分钟到250字每分钟之间。
```

---

### 步骤7：高频词汇分析

#### 7.1 功能描述

**做什么**：从教师讲授文本中抽取出现频率最高的 Top50 词汇，生成词云数据；并基于高频词结果调用 AI 模型生成点评与建议。

**后端技术栈**：Go

**前端技术栈**：Flutter

**数据来源**：步骤2输出的 `utterances` 数组

**处理流程**：

```
utterances数组
    ↓
过滤 role="teacher" 的文本并合并
    ↓
中文分词 + 清洗过滤（停用词/长度/标点等）
    ↓
词频统计并排序
    ↓
取Top50生成词云数据（后端计算）
    ↓
将Top50词频作为参数嵌入提示词，调用AI大模型
    ↓
返回：词云数据 + AI点评/建议
```

#### 7.2 后端计算规则（词频统计）

##### 建议技术方案（Go）

- **中文分词**：建议使用 `gojieba`（jieba 的 Go 封装）进行分词。
- **停用词表**：建议使用“哈工大停用词表（HIT stopwords）”作为通用基础，并叠加课堂场景的自定义停用词进行迭代。
  - 参考链接：
    - [https://github.com/goto456/stopwords/blob/master/hit_stopwords.txt](https://github.com/goto456/stopwords/blob/master/hit_stopwords.txt)

##### 统计范围

- 仅统计：`utterances` 中 `role = "teacher"` 的 `text`
- 统计单位：词（token）

##### 分词与清洗规则

后端对教师文本进行中文分词后，按以下规则过滤：

1. **停用词过滤**：过滤常见虚词、口头禅、称呼词。停用词表应支持配置和迭代。
2. **长度过滤**：默认过滤长度为1的词，避免信息量过低。
3. **字符类型过滤**：过滤纯标点、纯空白、无意义符号；纯数字可默认过滤。
4. **归一化**（可选）：统一全角/半角、大小写等。

##### 词频统计与排序

- 词频 `count`：某词在 teacher 文本中出现次数（按分词 token 计数）
- 取 Top50：按 `count` 从高到低排序，取前50个词

---

#### 7.3 AI 提示词

##### 系统提示词

```
你是一个专业的课堂教学分析助手。

【核心任务】
根据教师讲授的高频词统计结果，给出对本节课讲授内容侧重点的点评，并提出1-3条可执行的优化建议。

【输入数据说明】
输入包含：
- 高频词 Top50 列表：每项包含词语 text 和出现次数 count

【输出格式】（严格遵守）
{
  "ai_analysis": {
    "comment": "点评，30-150字",
    "suggestion": "建议，30-100字；无建议时可为null"
  }
}

【关键规则】
1. 点评和建议必须结合高频词结果，不要泛泛而谈
2. comment 必须输出，suggestion 可为 null
3. 不要输出除 JSON 外的任何内容
```

##### 用户提示词

```
请根据以下高频词统计结果，对教师讲授内容进行分析。

【高频词Top50】
{{topWords}}

请严格按系统提示词中的格式输出JSON，不要包含任何其他内容。
```

---

#### 7.4 输出数据结构（混合：后端计算 + AI模型生成）

**JSON 结构示例**：

```json
{
  "highFrequencyWords": {
    "words": [
      { "text": "一元二次方程", "count": 86 },
      { "text": "一般形式", "count": 52 },
      { "text": "解", "count": 49 }
    ],
    "ai_analysis": {
      "comment": "教师讲授中高频出现\"一元二次方程\"、\"一般形式\"、\"解\"等词，说明本节课围绕核心概念与基本表述展开，概念讲解密度较高。",
      "suggestion": "建议在概念讲解后增加更明确的例题类型归纳与变式练习，引导学生把概念迁移到解题步骤中。"
    }
  }
}
```

**字段说明**：


| 字段                             | 类型          | 来源     | 说明          |
| ------------------------------ | ----------- | ------ | ----------- |
| highFrequencyWords             | object      | 后端+AI  | 高频词模块整体数据   |
| highFrequencyWords.words       | array       | 后端计算   | Top50 高频词列表 |
| words[].text                   | string      | 后端计算   | 词语          |
| words[].count                  | int         | 后端计算   | 出现次数        |
| highFrequencyWords.ai_analysis | object      | AI模型生成 | 点评与建议       |
| ai_analysis.comment            | string      | AI模型生成 | 点评（必填）      |
| ai_analysis.suggestion         | string/null | AI模型生成 | 建议（可为null）  |


---

#### 7.5 前端渲染需求

##### 词云展示

- 使用 `highFrequencyWords.words` 渲染词云
- 展示教师讲授内容中出现最多的 Top50 词
- 词的视觉权重（大小）应与 `count` 正相关，且整体展示应保证可读性（避免少数词过大、其余词不可见）
- 支持基础交互：点击词语可查看该词的出现次数（count）（可选）

##### AI 分析展示

- 展示 AI 分析文本块，包含：
  - 点评：`highFrequencyWords.ai_analysis.comment`
  - 建议：`highFrequencyWords.ai_analysis.suggestion`（为 null 时不展示）

---

### 步骤8：师生时长占比分析

#### 8.1 功能描述

**做什么**：统计课堂中教师与学生的说话时长，并从两个维度进行展示：

1. 全课堂维度：教师/学生说话时长占比（饼图）
2. 教学环节维度：各主教学环节内教师/学生说话时长（堆叠柱状图）

并基于统计数据调用 AI 模型生成“评价 + 建议”。

**数据来源**：

- `utterances`：步骤2输出的师生分角色语音转写结果（含 start_ms/end_ms/role）
- `teaching_phases`：步骤3输出的教学环节（含各主环节的起止时间段）

**处理流程**：

```
utterances + teaching_phases
    ↓
计算全课堂 teacher/student 总说话时长
    ↓
按主教学环节统计 teacher/student 说话时长
    ↓
生成：饼图数据 + 堆叠柱数据
    ↓
将统计结果作为参数嵌入提示词，调用AI大模型
    ↓
返回：图表数据 + AI评价与建议
```

---

#### 8.2 计算规则

##### 8.2.1 全课堂师生说话时长

- 过滤条件：仅统计 `role ∈ {teacher, student}` 的 utterances
- 单句时长：`duration_ms = end_ms - start_ms`
  - 若 `duration_ms <= 0` 视为异常数据，忽略
- 统计：
  - `teacher_total_ms = sum(duration_ms where role=teacher)`
  - `student_total_ms = sum(duration_ms where role=student)`
- 占比计算（饼图）：
  - 分母：`speaking_total_ms = teacher_total_ms + student_total_ms`
  - `teacher_ratio = teacher_total_ms / speaking_total_ms`
  - `student_ratio = student_total_ms / speaking_total_ms`

说明：占比的分母使用“师生说话总时长”，不包含课堂中无人说话的空档时段。

##### 8.2.2 各主教学环节内师生说话时长

- 统计粒度：使用 `teaching_phases` 中的**主环节**作为柱状图粒度
- 环节时间段：每个主环节具有起始/结束时间点
- 统计口径：
  - 对于每个主环节区间内的 utterances，分别累加 teacher/student 的 `duration_ms`
  - 生成该环节：`teacher_ms`、`student_ms`
- 环节内占比（用于 tooltip 展示）：
  - 分母：`phase_speaking_ms = teacher_ms + student_ms`
  - `teacher_ratio = teacher_ms / phase_speaking_ms`
  - `student_ratio = student_ms / phase_speaking_ms`

说明：环节内占比的分母同样使用“该环节内师生说话总时长”，不包含该环节内的沉默/过渡空档。

---

#### 8.3 输出数据结构（混合：后端计算 + AI模型生成）

**JSON 结构示例**：

```json
{
  "behaviorRatio": {
    "overall": {
      "teacher_ms": 1284000,
      "student_ms": 356000,
      "teacher_ratio": 0.783,
      "student_ratio": 0.217
    },
    "byPhase": [
      {
        "name": "课堂导入",
        "teacher_ms": 240000,
        "student_ms": 60000,
        "teacher_ratio": 0.8,
        "student_ratio": 0.2
      },
      {
        "name": "新课讲解",
        "teacher_ms": 720000,
        "student_ms": 160000,
        "teacher_ratio": 0.818,
        "student_ratio": 0.182
      }
    ],
    "ai_analysis": {
      "comment": "老师说话21.4分钟，学生说话5.9分钟，整体上教师讲授占比较高，学生参与主要集中在提问与简短回答环节。",
      "suggestion": "建议在新课讲解环节增加更多追问与讨论式提问，并在课堂总结时安排学生复述关键结论，以提升学生表达与思维参与度。"
    }
  }
}
```

**字段说明**：


| 字段                        | 类型          | 来源     | 说明                                   |
| ------------------------- | ----------- | ------ | ------------------------------------ |
| behaviorRatio             | object      | 后端+AI  | 师生时长占比模块整体数据                         |
| behaviorRatio.overall     | object      | 后端计算   | 全课堂维度师生说话时长与占比                       |
| overall.teacher_ms        | int         | 后端计算   | 教师说话总时长（毫秒）                          |
| overall.student_ms        | int         | 后端计算   | 学生说话总时长（毫秒）                          |
| overall.teacher_ratio     | float       | 后端计算   | 教师说话占比（分母为 teacher_ms + student_ms）  |
| overall.student_ratio     | float       | 后端计算   | 学生说话占比（分母为 teacher_ms + student_ms）  |
| behaviorRatio.byPhase     | array       | 后端计算   | 各主环节的师生说话时长与占比                       |
| byPhase[].name            | string      | 后端计算   | 主环节名称                                |
| byPhase[].teacher_ms      | int         | 后端计算   | 该环节教师说话时长（毫秒）                        |
| byPhase[].student_ms      | int         | 后端计算   | 该环节学生说话时长（毫秒）                        |
| byPhase[].teacher_ratio   | float       | 后端计算   | 该环节教师占比（分母为 teacher_ms + student_ms） |
| byPhase[].student_ratio   | float       | 后端计算   | 该环节学生占比（分母为 teacher_ms + student_ms） |
| behaviorRatio.ai_analysis | object      | AI模型生成 | AI评价与建议                              |
| ai_analysis.comment       | string      | AI模型生成 | 评价（必填，必须以“老师说话X分钟，学生说话Y分钟，”开头）       |
| ai_analysis.suggestion    | string/null | AI模型生成 | 建议（可为 null）                          |


---

#### 8.4 AI 提示词

##### 系统提示词

```
你是一个专业的课堂教学分析助手。

【核心任务】
根据师生说话时长统计数据，输出“评价 + 建议”。

【输入数据说明】
输入包含：
- teacher_total_ms：教师总说话时长（毫秒）
- student_total_ms：学生总说话时长（毫秒）
- byPhase：按主环节统计的师生说话时长数组（结构见 8.3 的 `behaviorRatio.byPhase`）

【输出格式】（严格遵守）
{
  "ai_analysis": {
    "comment": "评价，必须以：老师说话X分钟，学生说话Y分钟， 开头",30-150字。
    "suggestion": "建议，30-100字；无建议时可为null"
  }
}

【关键规则】
1. comment 必须输出，suggestion 可为 null
2. comment 必须以固定格式开头：老师说话X分钟，学生说话Y分钟，
   - X、Y 必须由输入的 teacher_total_ms、student_total_ms 换算得到，不得编造
3. comment 在开头句式之后，应输出“点评/结论”，不要在 comment 中罗列过多具体环节的数值、比例（如 80%、81.8%、3.6:1 等）
   - 可以用概括性表达，例如："老师说话时长远多于学生"、"多数环节教师发言占优"、"个别环节学生参与度尚可" 等
4. comment 可点名 1-2 个环节作定性描述，但避免出现过多数字
5. suggestion 需要结合 byPhase 的分布给出 1-3 条可执行建议
6. 不要输出除 JSON 外的任何内容
```

##### 用户提示词

```
请根据以下师生说话时长统计结果进行分析。

【teacher_total_ms】{{teacher_total_ms}}
【student_total_ms】{{student_total_ms}}

【byPhase】
{{byPhase}}

其中 `byPhase` 的结构为数组，每项包含：
- name：主环节名称
- teacher_ms：该环节教师说话时长（毫秒）
- student_ms：该环节学生说话时长（毫秒）
- teacher_ratio：该环节教师占比（分母为 teacher_ms + student_ms）
- student_ratio：该环节学生占比（分母为 teacher_ms + student_ms）

请严格按系统提示词中的格式输出JSON，不要包含任何其他内容。
```

---

#### 8.5 前端渲染需求

##### AI 分析文本块

- 展示 AI 分析文本块，包含：
  - 评价：`behaviorRatio.ai_analysis.comment`
  - 建议：`behaviorRatio.ai_analysis.suggestion`（为 null 时不展示）

##### 师生说话时长占比饼图

- 展示教师/学生说话时长占比的饼图
- 饼图数据来源：`behaviorRatio.overall`
- 默认展示：教师占比、学生占比
- 鼠标/手势悬停（或点击）时，展示：教师说话时长、学生说话时长

##### 教学环节师生时长堆叠柱状图

- X轴：主教学环节名称（`behaviorRatio.byPhase[].name`）
- Y轴：说话时长
- 每个主环节柱体为堆叠结构：教师时长 + 学生时长
- 鼠标/手势悬停（或点击）时，展示该环节：
  - 教师说话时长、学生说话时长
  - 教师占比、学生占比（分母为该环节 teacher_ms + student_ms）

