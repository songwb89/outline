# 课堂分析工具 - 产品需求文档

> 本文档记录产品实现步骤，用于与开发团队沟通。
>
> 更新时间：2026-05-22
>
> 当前进度：已完成步骤1、2

---

## 功能概述

将课堂录音/视频自动转成带角色标注的文字稿，方便后续分析学生学习情况。

---

## 实现步骤

### 步骤1：音频识别（语音转文字）

**做什么**：把用户上传的音视频文件，转换成文字内容。

**用户操作**：

1. 用户上传课堂录音（mp3/wav/m4a）或课堂视频（mp4/avi）
2. 系统自动处理，生成文字稿

**处理流程**：

```
用户上传文件
    ↓
如果是视频 → 用工具提取音频
    ↓
调用火山引擎ASR服务（豆包大模型版）
    ↓
生成原始识别结果（JSON格式）
```

**输出**：原始识别JSON文件，包含所有识别出的文字和时间戳

**技术说明**（供开发参考）：

- 视频格式使用 FFmpeg 提取音频
- 使用火山引擎 `volc.bigasr.auc` 服务
- 模型名称：`bigmodel`（大模型版）
- 配置参数：开启标点预测、逆文本归一化、说话人分离

---

### 步骤2：数据预处理与角色识别

**做什么**：把原始识别结果整理干净，并自动识别出哪个说话人是老师。

**处理流程**：

```
原始识别JSON
    ↓
清理数据（去除逐字细节，保留完整句子）
    ↓
提取包含多个说话人的对话片段
    ↓
调用AI大模型（阿里千问）判断哪个是老师
    ↓
生成干净的带角色标注的数据
```

**输出**：处理后JSON文件

**数据结构示例**：

```json
{
  "utterances": [
    {
      "start": 20890,
      "end": 24930,
      "text": "已经在的同学打开书，背一下春江花月夜啊，自己。",
      "role": "teacher"
    },
    {
      "start": 207340,
      "end": 209620,
      "text": "老师好！",
      "role": "student"
    }
  ]
}
```

---



